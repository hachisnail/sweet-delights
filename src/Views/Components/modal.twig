<div id="alertModal"
  class="hidden fixed inset-0 bg-black/40 backdrop-blur-sm z-999 flex items-center justify-center">
  <div class="bg-white rounded-2xl shadow-xl p-6 max-w-sm w-full text-center relative overflow-hidden">
    
    <div id="alertModalIcon" class="mb-4 mx-auto w-12 h-12 flex items-center justify-start rounded-full">
    </div>

    <h2 id="alertModalTitle" class="text-xl font-bold text-gray-800 mb-2">Notification</h2>

    <p id="alertModalMessage" class="text-gray-600 mb-6">This is an alert message.</p>

    <div id="alertModalButtons" class="flex justify-end gap-4"></div>
  </div>
</div>
